[build]
# Specify Node.js version
nixpacks_version = "1.38.0"

[providers]
# Use Node.js provider
node = "20"

[phases.build]
# Build command for backend
cmds = ["cd backend && npm install"]

[phases.start]
# Start command for backend
cmd = "cd backend && npm start"
